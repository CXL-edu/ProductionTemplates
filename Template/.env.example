# 环境变量配置文件示例
# 复制此文件为 .env 并修改相应的值

# Agent 工具 API 配置
## OpenCode API Key
OPENCODE_API_KEY=YOUR_OPENCODE_API_KEY_HERE
## Claude API 配置
ANTHROPIC_AUTH_TOKEN=your_auth_token_here
ANTHROPIC_BASE_URL=https://open.bigmodel.cn/api/anthropic

# LLM API 配置
## NVIDIA LLM API 配置
NVIDIA_LLM_URL=https://integrate.api.nvidia.com/v1
NVIDIA_LLM_KEY=YOUR_NVIDIA_LLM_KEY_HERE



# Search API 配置
## Brave Search API 配置
BRAVE_API_KEY=YOUR_API_KEY_HERE
## Exa Search API 配置
EXA_API_KEY=YOUR_EXA_API_KEY_HERE
## Serper Search API 配置
SERPER_API_KEY=YOUR_SERPER_API_KEY_HERE

# Git 配置（可选，容器启动时自动配置）
GIT_USER_NAME=your-name
GIT_USER_EMAIL=your-email@example.com
## Github PAT
GITHUB_PERSONAL_ACCESS_TOKEN=YOUR_Github_PAT

# 浏览器 MCP 配置（是否安装浏览器相关依赖和启用浏览器 MCP）
ENABLE_BROWSER_MCP=false

# Miniforge 版本配置
# 指定要安装的 Miniforge 版本，建议使用最新稳定版
# 如果 assets 目录中有对应版本的安装包，将优先使用本地文件
MINIFORGE_VERSION=25.11.0-1


# Clash 配置（仅在 PROXY_MODE=tun 时使用）
# Clash 配置文件下载 URL（包含敏感 token，请妥善保管）
CLASH_CONFIG_URL=YOUR_CLASH_CONFIG_URL_HERE

# 代理模式配置
# 可选值：
#   none - 无代理模式，不使用任何代理
#   auto - 自动代理模式，使用 HTTP_PROXY/HTTPS_PROXY 环境变量配置的代理，并自动为 MCP 服务器注入代理支持
#          （通过 proxy-inject.js 脚本，解决某些 MCP 使用 node fetch 不走代理的问题）
#   tun  - TUN 模式，使用系统级透明代理（如 Clash TUN 模式），不需要应用层代理配置
#          注意：TUN 模式下不会使用 proxy-inject.js，因为系统级代理已自动处理所有流量
PROXY_MODE=tun

# 配置代理（仅在 PROXY_MODE=auto 时生效）
# clash 默认端口为 7890，如果是 v2ray，则端口为 1080
HTTP_PROXY=http://127.0.0.1:7890
HTTPS_PROXY=http://127.0.0.1:7890

# Docker Volumes 挂载路径配置（可选）
# 挂载策略：
#   - 如果不设置此变量，默认使用项目目录下的相对路径 ./docker-volumes
#   - 如果项目目录下没有 docker-volumes 文件夹，可以设置此变量为绝对路径
#   - 例如：DOCKER_VOLUMES_PATH=/root/project/docker-volumes
# 注意：如果项目目录下存在 docker-volumes 文件夹，建议不设置此变量，使用默认的相对路径
# DOCKER_VOLUMES_PATH=/root/project/docker-volumes